import logging

logger = logging.getLogger(__name__)

def tree_compare(old_dict, new_dict):
    # Comparing two nexted dictionaries and generating before/after state
    actions = list()
    for key, value in dict1:

    return actions

def _key_check(value1, value2):